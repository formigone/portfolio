{
  "name": "rscom-hello-world",
  "version": "1.0.0",
  "description": "Template project for standalone demos",
  "scripts": {
    "watch": "webpack -w --define process.env.APP_ENV=development --progress --colors",
    "build": "webpack -p --define process.env.NODE_ENV=production --progress --colors",
    "dev": "./../../node_modules/webpack-dev-server/bin/webpack-dev-server.js --content-base public --hot --inline --progress --colors",
    "test": "./../../node_modules/mocha/bin/mocha --require babel-core/register tests/**/**.js"
  }
}
